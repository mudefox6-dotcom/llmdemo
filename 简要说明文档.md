# 多智能体需求交付系统 —— 简要说明文档

> 一句话：输入一段自然语言需求，输出一整套可交付物（PRD、技术方案、接口与数据模型设计、评审报告），并附带一个**能直接跑起来的前后端项目骨架**。
>
> 线上地址：https://llm.beautymodel.com.cn （需登录）
> 代码仓库：https://github.com/mudefox6-dotcom/llmdemo

---

## 一、实现思路

### 1.1 为什么做成"多智能体"而不是一个大 Prompt

把需求变成交付物这件事，在真实团队里本来就是**多个角色分工 + 互相评审**：产品经理写 PRD、架构师出方案、开发定接口和表结构、评审人挑问题。

用一个大 Prompt 一次生成，会遇到三个绕不过去的问题：

1. **输出长度撞墙**。一个中等需求的完整交付物轻松上万 token，单次生成必然被 `max_tokens` 截断（这个坑实际踩到了：`finish_reason=length`，工具调用返回空）。
2. **无法定位错误**。生成结果不对，你不知道是"需求理解错了"还是"表结构设计错了"，只能整体重来。
3. **没有质量兜底**。模型不会主动说"我这里漏了"，需要一个独立角色专门找问题。

所以拆成职责单一的智能体，让**流程**而不是**单次生成**来保证质量。

### 1.2 整体架构

```
浏览器 (Vue 3 + Vite)
   │  REST 提交需求 / SSE 接收实时进度
   ▼
FastAPI
   │  任务入队（asyncio 队列，多 worker 并发）
   ▼
LangGraph StateGraph  ←──→  SQLite Checkpointer（断点续跑）
   │  黑板式共享状态 AgentState 在节点间流转
   ▼
交付物落盘 output/{task_id}/ + 可运行项目骨架 zip
```

技术选型与理由：

| 层次 | 选型 | 为什么 |
|---|---|---|
| 编排 | **LangGraph 1.0** StateGraph | 需要"有环图"（评审不通过要回流重做）和**人工介入后从断点恢复**，这两点用普通的链式调用做不了 |
| 模型接入 | **LangChain 1.x** + OpenAI 兼容协议 | `with_structured_output` 让模型直接产出 Pydantic 对象，省掉解析 JSON 的脏活；兼容协议使换供应商只改 `.env` |
| 模型 | 硅基流动 `deepseek-ai/DeepSeek-V4-Flash` | 中文需求理解够用、函数调用稳定、单价与延迟适合演示（曾用过通义千问、DeepSeek 官方，切换只改配置） |
| 后端 | FastAPI + SSE | Agent 跑几分钟，必须能把中间进度推给前端，否则用户面对空白页无法判断是在跑还是卡死 |
| 持久化 | SQLite（业务）+ SQLite（checkpoint）+ Chroma（长期记忆，可选） | 单机演示零运维；接口层做了抽象，换 Postgres 只改连接串 |
| 前端 | Vue 3 + Pinia + Element Plus | 组合式 API 写实时进度这类有状态 UI 更顺手 |

### 1.3 智能体分工与流转

```
START
 └─ input_normalize    需求归一化：清洗输入、抽取原始诉求
 └─ planner            规划：产出结构化需求树 + PRD 骨架
 └─ plan_evaluator     计划自检：判断信息是否足够
      ├─ 信息不足 ──▶ human_clarification ──▶ 回到 planner
      └─ 信息充分 ──▶ solution
 └─ solution           技术方案：架构选型、模块拆分、风险
 └─ engineer           工程实现：API 清单、数据表、字段约束
 └─ reviewer           评审：规则门禁 + 模型评审，给出问题清单
      ├─ 有问题 ──▶ repairer ──▶ 回到 reviewer（最多 MAX_REFLOW_COUNT 轮）
      └─ 通过   ──▶ human_approval
 └─ human_approval     人工确认（LangGraph interrupt 真正挂起）
      ├─ 驳回 ──▶ 按意见回流到对应节点
      └─ 批准 ──▶ package_output
 └─ package_output     导出 Markdown 交付物 + 生成可运行项目 zip
END
```

设计上的几个关键决定：

- **黑板模式**：所有智能体不直接互相调用，只读写同一个 `AgentState`（TypedDict）。谁产出什么字段、谁消费什么字段是显式声明的，加一个智能体不用改其他智能体。
- **reducer 控制写入语义**：`_replace` 表示"后写覆盖先写"，`_append_list` 表示"各节点追加不互相踩"。并行节点同时写同一字段时不会丢数据。
- **评审回流有上限**：`MAX_REFLOW_COUNT` 封顶，避免"评审—修复—评审"无限循环烧 token。
- **人工介入用 `interrupt()` 真挂起**：不是前端假装等待。图的状态落盘到 checkpoint，进程重启也能用 `Command(resume=...)` 从原地继续。
- **工程实现节点做了分批**：模块数超过阈值时，先出架构骨架，再把模块分批并行生成，最后归一化去重。这是为了绕开单次输出 token 上限——也是从"生成结果总是被截断"这个真实故障里逼出来的方案。

### 1.4 交付物不只是文档

早期版本只输出 Markdown，但"设计得对不对"光看文档看不出来。所以增加了 `scaffold_generator`：把 engineer 产出的数据表和接口清单，**确定性地**（模板渲染，不再过模型）生成一个真项目：

- 后端 FastAPI + SQLAlchemy，按设计的表结构建模型、生成可用的 CRUD 接口
- 前端 Vue 页面是**业务功能界面**（列表、表单、增删改查），不是 API 文档目录
- 附 README，解压后本地两条命令就能起来

为什么这一步不用模型：模型生成代码有语法错误概率，而这一步的输入已经是结构化数据，模板渲染能保证 100% 可运行。**能用确定性代码解决的，不交给模型。**

---

## 二、当前完成度

### ✅ 已完成并验证可用

| 模块 | 状态 |
|---|---|
| LangGraph 编排（11 个节点、条件路由、回流） | 完成 |
| 需求归一化 / 规划 / 方案 / 工程实现 / 评审 / 修复 六类智能体 | 完成 |
| 结构化输出（Pydantic schema 约束）+ 失败重试与三级降级 | 完成 |
| 人工澄清 + 人工审批（interrupt / resume 断点续跑） | 完成 |
| SQLite Checkpointer（增量写入，重启可恢复） | 完成 |
| 任务队列（多 worker 并发、任务删除、队列位置提示） | 完成 |
| SSE 实时进度推送（含断线重连、退避轮询兜底） | 完成 |
| 交付物导出（PRD / 技术方案 / 详细设计 / 评审报告 Markdown） | 完成 |
| **可运行项目骨架生成**（FastAPI + Vue，含真实 CRUD 页面） | 完成 |
| 账号密码登录（HMAC 签名 token，前后端打通） | 完成 |
| 演示模式（DEMO_MODE，压低单轮耗时） | 完成 |
| Docker 化部署 + HTTPS 上线 | 完成 |

### ⚙️ 已实现但默认关闭（开关在 `.env`）

这些是探索性能力，效果有提升但会明显拉长耗时，演示时关掉：

- `MEMORY_ENABLED` —— Chroma 向量长期记忆 + few-shot 检索 + 记忆整合去冲突
- `DIALOGUE_ENABLED` —— 评审人与开发者多轮对话式协商（而非一次性评审）
- `PLAN_VARIANTS_ENABLED` —— 生成多个方案变体后择优
- `SELF_REFINE_ENABLED` —— 节点级自我修正

### 📊 实测性能

- 中等需求端到端：**约 200 秒**（DEMO_MODE + V4-Flash）
- 优化前 **520 秒 → 358 秒 → 203 秒**。三步分别来自：关掉 self-refine、把评审回流上限降到 0、换更快的模型 + 限制模块数
- 瓶颈是**串行的 5 次大模型调用**，这是流程决定的下限。演示时建议预跑一个任务，现场展示已完成结果 + 现场发起一个新任务看实时进度

### ⚠️ 已知限制（说明书里不藏问题）

1. **单账号认证**。只有一组账号密码，没有用户体系、没有权限分级。演示够用，多人协作不够。
2. **SQLite 单机**。并发写有锁竞争；多实例部署需要换 Postgres。
3. **生成项目的深度有限**。骨架是可运行的 CRUD，但复杂业务逻辑（工作流、事务、权限）不会生成。
4. **SSE token 走 query 参数**。浏览器 `EventSource` 不支持自定义请求头，token 会出现在 nginx 访问日志里，靠 12 小时有效期限制影响面。正规做法是换 WebSocket 或短时一次性票据。
5. **没有自动化测试套件**。关键路径是手工 + 一次性脚本验证的，没有 CI。

---

## 三、AI Coding 工具使用说明

本项目从零到上线，全程使用 **Claude Code**（Anthropic 官方 CLI，接入 VS Code）作为主要开发工具。下面写的是实际用法和真实体会，不是功能列表。

### 3.1 分工方式

| 环节 | 谁主导 | 说明 |
|---|---|---|
| 架构与技术选型 | **人** | 拆几个智能体、用不用 LangGraph、状态怎么设计——这些定错了后面全白干，AI 给不了取舍判断 |
| 骨架代码、模板渲染、CRUD 这类样板 | **AI** | `scaffold_generator.py` 近千行模板代码，人写非常枯燥，AI 一次成型 |
| 疑难 Bug 定位 | **人 + AI** | AI 擅长快速加诊断代码、读长堆栈；判断"这是不是真正的根因"要人来做 |
| 代码注释与文档 | **AI** | 全项目中文注释（含字段业务含义）由 AI 补全 |
| 部署运维 | **AI 执行，人决策** | AI 直接 SSH 上服务器操作，但"停旧容器还是删旧容器"由人拍板 |

### 3.2 用得最有效的三种方式

**① 让它先读代码再改，而不是直接给方案。**
每次提需求前先让它把相关文件读一遍。省掉大量"生成的代码风格与现有代码不一致""调用了不存在的函数"的返工。

**② 报错时把完整原始输出贴给它，不做人工概括。**
最典型的一次：Docker 构建失败，我一开始概括成"网络问题"，AI 就跟着往镜像源方向修，改了三轮都没用。后来直接把 `--progress=plain` 的完整日志贴过去，真实原因是 `npm error EUSAGE Missing: @emnapi/core from lock file`——Windows 生成的 lockfile 缺 Linux 平台的可选依赖。**人的概括会把关键线索概括掉。**

**③ 要求它验证，而不是相信它的结论。**
AI 会说"已修复"。所以约定：改完必须跑一次实际验证并把输出贴出来。认证功能就是这么定下来的——11 项检查逐条跑通才算完成。这个习惯抓出过好几个"看起来对但实际错"的改动，比如 JS 里 `{}` 是真值导致前端显示 `undefined`。

### 3.3 踩过的坑（AI 协作层面）

- **AI 会自信地说错话。** 它曾断言"`SystemMessage()` 报错是 `@overload` 导致的"、"枚举和字符串比较会失败"。我让它写脚本实测，两次结论都被推翻，真实原因分别是 Pydantic 校验和 f-string 输出格式。**结论必须可复现，不能只听解释。**
- **AI 会声称做了没做的事。** 多次说"已同步到文档"，实际文件没变。后来要求每次操作后回读文件确认。
- **管道会吞掉错误。** `命令 | tail` 会丢掉退出码，让失败看起来像成功。调试时不要加管道。
- **长上下文会遗忘早期约定。** 项目规范、命名习惯这类东西要落成文件，靠对话记忆不可靠。

### 3.4 一句话总结

AI Coding 工具真正的价值不是"帮我写代码"，而是**把我的时间从样板代码里挪到架构判断和验证上**。它写得快，但对不对得由人负责——所以流程上必须有"必须实测验证"这一环。这一点和本项目自身的设计思路完全一致：**用流程保证质量，而不是指望单次生成就对。**

---

## 四、如果继续投入会如何扩展

按投入产出排序，从最该做的开始。

### 第一优先：让质量可度量（1～2 周）

现在"生成得好不好"靠人看，这是最大的短板。

- 建**评测集**：30～50 个真实需求，人工标注期望产出（该有哪些模块、哪些表、哪些接口）
- 定**自动指标**：需求覆盖率、接口/表设计完整度、评审问题命中率、生成项目能否成功启动
- 做**回归基线**：改 Prompt、换模型后自动跑全集对比。现在换模型只能靠感觉判断好坏

**为什么排第一**：没有度量，后面所有优化都是盲改。

### 第二优先：工程化补齐（1～2 周）

- **pytest 测试套件** + GitHub Actions CI（现在是手工验证）
- **Postgres 替代 SQLite**，支持多实例
- **结构化日志 + 链路追踪**：每个任务每个节点的耗时、token 消耗、重试次数可查（`observability.py` 有基础，需要接入面板）
- **成本核算**：按任务统计 token 花费，这是生产环境的硬需求

### 第三优先：能力纵深（3～4 周）

- **把默认关闭的能力做到可以默认开启**：长期记忆、多轮协商、方案择优现在效果有提升但太慢。方向是分级——快速模式走单轮，深度模式开全量
- **代码生成从骨架走向可用**：现在只生成 CRUD。下一步接入代码执行沙箱，让生成的项目**自己跑测试**，失败了自动修复再重试——把"评审—修复"闭环从文档层面延伸到代码层面
- **引入检索增强**：接入企业内部的现有 API 文档、数据库 schema、编码规范，让产出与既有系统对齐，而不是每次从零设计

### 第四优先：产品化（4 周以上）

- **多用户 + 权限**：项目空间、成员角色、交付物版本历史（当前单账号）
- **协作介入**：现在人工介入只有"批准/驳回"，应该支持直接编辑中间产物再继续
- **对接研发流程**：一键推送到 Jira/禅道建任务、生成 Git 仓库并提 PR、导出到 Confluence
- **模型路由**：简单节点用小模型、关键节点用大模型，按节点选型压成本

### 更长期的一个判断

当前是**固定流程图 + 固定角色**。下一代形态应该是**让编排本身动态化**：由一个 Orchestrator 根据需求复杂度决定派哪些智能体、要不要加轮次、要不要引入领域专家角色（`orchestrator_agent.py` 已有雏形）。

固定流程的好处是稳定可预测，坏处是简单需求也走全套、复杂需求又不够深。动态编排能解决这个矛盾，但前提是**第一优先级的评测体系必须先建起来**——否则无法判断动态决策到底是变好还是变坏。

---

## 附：部署与运行

### 线上访问

https://llm.beautymodel.com.cn

演示账号 `admin` / 演示密码 `admin@123` —— 登录页上直接标注了，点「一键填入」即可。
这是演示站点，刻意不设访问门槛；认证机制本身是完整的（服务端校验 + HMAC 签名 token），
换到正式环境只需改服务器 `.env` 里的 `AUTH_USERNAME` / `AUTH_PASSWORD` / `AUTH_SECRET`。

### 本地运行（Windows PowerShell，无需 Docker）

```powershell
# 1) 后端
pip install -r requirements.txt
Copy-Item .env.example .env      # 填入 OPENAI_API_KEY
python -m uvicorn app.main:app --reload --port 8000

# 2) 前端（另开一个终端）
cd frontend-vue
npm install
npm run dev
```

浏览器打开 http://localhost:5173 。本地调试可在 `.env` 设 `AUTH_ENABLED=false` 跳过登录。

### 服务器部署

```bash
cd /opt/llmdemo
git pull
docker compose -f docker-compose.prod.yml up -d --build
```

外层 nginx（`beauty-site-nginx`）按容器名 `llmdemo-nginx` 反代该域名，TLS 证书与 SSE 所需的 `proxy_buffering off`、600s 超时已配置。
